React App (Vite)

A modern React application bootstrapped with Vite, built for fast development, clean structure, and scalable styling. This project demonstrates a simple React setup with modular CSS and a minimal entry point.

🚀 Tech Stack

React – Component-based UI library

Vite – Fast build tool and dev server

JavaScript (ES6+)

CSS – Modular and global styling


Project Structure
├── src
│   ├── App.jsx        # Main React component
│   ├── App.css        # Styles for App component
│   ├── main.jsx       # Application entry point
│   ├── index.css      # Global styles
│   └── styles.css     # Additional shared styles
├── index.html
├── package.json
└── vite.config.js


How It Works ; 

main.jsx mounts the React app to the DOM

App.jsx contains the main UI logic

Styling is split into:

index.css → global styles

App.css → component-specific styles

styles.css → reusable/common styles
